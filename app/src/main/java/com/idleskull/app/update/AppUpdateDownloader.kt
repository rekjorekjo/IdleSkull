package com.idleskull.app.update

/**
 * Kept as a source-compatible tombstone for old patches.
 * APK downloads are handled by Android's DownloadManager in [AppUpdateManager].
 */
@Deprecated("Use AppUpdateManager")
internal object AppUpdateDownloader
